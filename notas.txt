 let y const son mas restrictivas, depente de donde se pongan son globales o locales y hay que evitar utilizar VAR
 Capturar datos: promt() se pueden capturar datos por teclado 
 mostrar datos: cosole.log() o aler()
 datos de proceso:Toda operación matematica o lógica que vayamos a aplicar 
 Variables JavaScript; 
 tipo de variable:let, const o var
 tipo de datos:
 1: number: cualquier número
 2: string: cualquier letra, palabra o frase
 3: null: es la definición de algo pero está vacio
 4: undefined: La utilizacón de una variable o contenido que no existe 
 5: object: Objetos y arreglos 
 6: "Nan" (Not at number) 
 7: Boleano: Valores lógicos que van a ser falso o verdadero

 typeof:
 parseo: 
 isNaN:

 NOTA: TODO DATO INGRESADO POR TECLADO (PRONT O FORMULARIOS)  !!!!!!!SIEMPRE¡¡¡¡¡¡ TENDRÁN UN TIPO DE DATO STRING

 SE RECOMIENDA hacer un parseo de datos siempre y cuando se involucre una operación matemática

 Parseo: El proceso de tranformación de un tipo de datos a otro (string a numero)
 Parseo de datos:
 String a numero:
 -Number():
 -parseInt():
 -parseFloat():
 numero a string:
 -String():
 -toString():

 Operadores aritéticos:
 ** potencia
 % mòdulo
 Incrementar:
 console.log(++a); incrementa el valor en uno 
 console.log(a++); imprime el valor de a que anteriormente lo incrementamos en uno y le suma otro màs
 console.log(a); acá muestra el valor incrementado en 2
 Decrementar
  console.log(--a); 
 console.log(a--); 
 console.log(a); 
 
 Operadores de asignación:
 a+= 5; acá decido en cuanto queremos incrementarlo, en lo anteior solo era de uno en uno.
 Operadores de comparación:
 == es igual
 != distinto 

 Operadores lógcios:
 AND-&&
 OR- || // está la saco con el alt124
 NOT- !

 short-circuit:
 Falso- false,0,'',null, undefined, NaN